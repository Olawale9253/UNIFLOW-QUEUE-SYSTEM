# Server Configuration
server.port=8081
server.servlet.context-path=/api

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/uniflow_db
spring.datasource.username=postgres
spring.datasource.password=Walex@2020
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

# Disable Flyway
spring.flyway.enabled=false

# JWT Configuration
app.jwt.secret=uniflowSecretKey2026ForJWTGenerationWithProperLength
app.jwt.expiration.ms=86400000

# CORS
app.cors.allowed-origins=http://localhost:3000

# ============================================
# EMAIL CONFIGURATION (Gmail)
# ============================================

# SMTP Settings
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=uniflow925@gmail.com
spring.mail.password=zumr jdtd qllc hbku
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ============================================
# LOGGING
# ============================================
logging.level.org.springframework.mail=DEBUG
logging.level.com.uniflow=DEBUG